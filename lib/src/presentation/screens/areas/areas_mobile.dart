import 'package:flutter/material.dart';

class AreaMobile extends StatelessWidget {
  const AreaMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}