import 'package:flutter/material.dart';

class EquipmentScheduleMobile extends StatelessWidget {
  const EquipmentScheduleMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}