import 'package:flutter/material.dart';

class PeriodsMobile extends StatelessWidget {
  const PeriodsMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}