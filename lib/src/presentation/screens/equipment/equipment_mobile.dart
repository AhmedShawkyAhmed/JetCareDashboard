import 'package:flutter/material.dart';

class EquipmentsMobile extends StatelessWidget {
  const EquipmentsMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}