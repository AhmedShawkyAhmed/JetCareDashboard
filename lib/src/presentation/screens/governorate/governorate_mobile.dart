import 'package:flutter/material.dart';

class GovernorateMobile extends StatelessWidget {
  const GovernorateMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}