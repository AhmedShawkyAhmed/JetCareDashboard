import 'package:flutter/material.dart';

class StatesMobile extends StatelessWidget {
  const StatesMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}