

import 'package:flutter/material.dart';

class CorporatesMobile extends StatelessWidget {
  const CorporatesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}